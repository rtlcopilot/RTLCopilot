version: "3.9"

services:
  pd-tools:
    image: rtlcopilot/pd-tools:latest
    build: .
    ports:
      - "7070:7070"
    volumes:
      - ./work:/work
      - ./api.py:/app/api.py
      - ./pd_verification.py:/app/pd_verification.py
    restart: unless-stopped